import React from 'react';
import CandidatesRouts from '../../../routes/candidates';

const HrCandidatesPage = (props: any) => {

    return (
        <div>
            <CandidatesRouts {...props} />
        </div>
    );
};

export default HrCandidatesPage;